[workspace]
members = [
    "apps/musicd",
    "apps/musicd-cli",
    "crates/musicd-core",
    "crates/musicd-upnp",
]
resolver = "2"

[workspace.package]
edition = "2024"
license = "MIT"
version = "2.1.0"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
